# Environment / secrets
.env
.env.*
!.env.example

# Python (bridge, observatory)
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Node
node_modules/
dist/

# Runtime state — never commit a running town
.identities/
run/
state/
logs/
*.log

# Experiments are the RESEARCHER'S data, not part of the software. The repo
# ships the apparatus, sensible defaults, and a documented example spec
# (experiments.example/); what you actually run belongs to you.
experiments/

# macOS
.DS_Store
